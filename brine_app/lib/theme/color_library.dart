import 'dart:ui';

import 'package:flutter/material.dart';

/// Fixed color values to be used across theme modes.
class ColorLibrary {
  static const Color primaryDefault = Color(0xFF222222);
  static const Color primaryLight = Color(0xE1FFFFFF);
}
