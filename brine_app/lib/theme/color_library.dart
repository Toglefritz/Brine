import 'dart:ui';

import 'package:flutter/material.dart';

/// Fixed color values to be used across theme modes.
class ColorLibrary {
  /// A color used for error messages and indicators.
  static const Color error = Color(0xFFA20000);
}
